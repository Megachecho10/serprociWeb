@model IEnumerable<SERPROCI.Models.Servicio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoServicio.TipoServicioName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ServicioName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescripcionServicio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoServicio.TipoServicioName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ServicioName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionServicio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdServicio }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdServicio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdServicio })
        </td>
    </tr>
}

</table>
