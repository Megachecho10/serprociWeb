@model IEnumerable<SERPROCI.Models.Umd>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UmdName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UmdName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdUmd }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdUmd }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdUmd })
        </td>
    </tr>
}

</table>
