@model IEnumerable<SERPROCI.Models.Almacen>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Empresa.EmpresaName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoAlmacen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreAlmacen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Empresa.EmpresaName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoAlmacen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreAlmacen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAlmacen }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAlmacen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAlmacen })
        </td>
    </tr>
}

</table>
